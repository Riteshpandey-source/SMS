# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=attendance_management
DB_USER=postgres
DB_PASSWORD=postgres
# Optional: use this instead of DB_HOST/DB_PORT/DB_NAME/DB_USER/DB_PASSWORD
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/attendance_management

JWT_SECRET=replace-with-a-strong-secret
JWT_EXPIRES_IN=1d
CORS_ORIGIN=http://localhost:5173
